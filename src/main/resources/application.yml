spring:
  application:
    name: terrarent
  # Indented correctly under 'spring:'
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/terrarent_db}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:2007}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER:org.postgresql.Driver}
#  datasource:
#    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/terrarent_db}
#    username: ${SPRING_DATASOURCE_USERNAME:postgres}
#    password: ${SPRING_DATASOURCE_PASSWORD:2007}
#    driver-class-name: ${SPRING_DATASOURCE_DRIVER:org.postgresql.Driver}
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update 
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    defer-datasource-initialization: true

application:
  security:
    jwt:
      secret-key: ${JWT_SECRET_KEY:aSuperSecretKeyForTerraRentApplicationWhichIsLongEnoughForHS256Algorithm}
      access-token-expiration: 7200000 
      refresh-token-expiration: 604800000 

bookingcom:
  api:
    base-url: ${BOOKING_COM_BASE_URL:https://demandapi-sandbox.booking.com/3.1}
    affiliate-id: ${BOOKING_COM_AFFILIATE_ID:YOUR_AFFILIATE_ID}
    token: ${BOOKING_COM_TOKEN:YOUR_API_TOKEN}

server:
  port: 8081

logging:
  file:
    name: backend.log
  level:
    com.terrarent: DEBUG
    org.springframework.security: INFO